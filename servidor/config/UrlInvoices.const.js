const UrlInvoices = {
  GetInvoices: "/facturas",
  PostInvoice: "/facturas",
  GetInvoice: "/facturas/:id",
  PutInvoice: "/facturas/:id",
  DeletInvoice: "/facturas/:id",
};

module.exports = UrlInvoices;
